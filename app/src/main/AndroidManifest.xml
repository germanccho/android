<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clinica.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity android:name=".Gestionar_OS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.View.ObraSocial.agregarObraSocial" />
        <activity android:name=".UI.View.ObraSocial.BuscarObraSocial" />
        <activity android:name=".UI.View.ObraSocial.EditarObraSocial" />
        <activity android:name=".UI.View.ObraSocial.ConfiguracionOS" />

    </application>

</manifest>